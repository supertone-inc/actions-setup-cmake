name: 'actions-setup-cmake'
description: 'Setup the cmake build generator'
inputs:
  cmake-version:
    description: 'The version of cmake to setup. E.g. 3.14.0'
    default: ''
  github-api-token:
    description: 'GitHub token to use when retrieving version info'
    default: '${{ github.token }}'
  architecture:
    description: 'Target architecture for cmake to setup. e.g. x64, x86, arm64. Will use system architecture by default.'
runs:
  using: 'node12'
  main: 'dist/index.js'
